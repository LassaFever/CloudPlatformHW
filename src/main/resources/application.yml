#thymeleaf
spring:
    thymeleaf:
        cache: false
        encoding: UTF-8
        mode: HTML5
        prefix: classpath:/templates/
        suffix: .html

    messages:
        basename: i18n.login

    servlet:
      multipart:
          max-file-size: 10MB
          max-request-size: 50MB
